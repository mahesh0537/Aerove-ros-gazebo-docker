To install gazebo in ROS2 run gazebo.sh <br>
with ROS gazebo comes by default in full-desktop mode <br>
to run docker and be able to use display replace the image name in run_docker.sh and run it <br>